import React from 'react'

import generics from '../../styles/generics/generics.module.scss'

const New: React.FC = () => {
    return (
        <h1 className={generics['text-center']}>Create a new Invoice here!</h1>
    )
}

export default New
